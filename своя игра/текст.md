в html


 <div id="мой айди">текст</div>


 в css


#мой айди {
  /* ========================================================== */
  /* 1. СТИЛЬ (Шрифт, размер, цвет)                             */
  /* ========================================================== */
  font-family: 'Arial', sans-serif; /* Название шрифта */
  font-size: 32px;                  /* Размер (больше цифра — больше текст) */
  color: #ff4757;                   /* Цвет текста */
  font-weight: bold;                /* Жирность (bold — жирный, normal — обычный) */

  /* ========================================================== */
  /* 2. УПРАВЛЕНИЕ МЕСТОМ (Вверх, вниз, влево, вправо)          */
  /* ========================================================== */
  position: relative; /* Включает режим свободного перемещения */
  
  top: 0px;    /* Двигает ВНИЗ (плюс, например: 20px) или ВВЕРХ (минус, например: -20px) */
  left: 0px;   /* Двигает ВПРАВО (плюс, например: 30px) или ВЛЕВО (минус, например: -30px) */
}